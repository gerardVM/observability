apiVersion: 1
contactPoints:
  - orgId: 1
    name: platform_email
    receivers:
      - type: email
        settings:
          addresses: valverdegerard@gmail.com
          message: '{{ template "teams.default.message" . }}'
          singleEmail: true
          subject: '{{ template "default.title" . }}'
        disableResolveMessage: false
